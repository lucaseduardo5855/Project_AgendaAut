import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'

export async function GET(request: Request) {
  const { searchParams, origin } = new URL(request.url)
  const code = searchParams.get('code')
  
  // Isso serve para tratar tanto cadastro quanto recuperação de senha
  const next = searchParams.get('next') ?? '/dashboard'

  if (code) {
    const supabase = await createClient()
    const { error } = await supabase.auth.exchangeCodeForSession(code)
    
    if (!error) {
      return NextResponse.redirect(`${origin}${next}`)
    }
  }

  // Se der erro ou link expirado (clicou duas vezes), joga pro Auth com erro.
  return NextResponse.redirect(`${origin}/auth?error=Link expirado ou já utilizado. Faça login.`)
}
